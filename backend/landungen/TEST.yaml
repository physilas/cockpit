# Nur für Engine-Tests: kurze, hindernisfreie Strecke, um Sieg-/Verlust-
# Logik ohne Zufallseinfluss durch Flugzeuge/Kollisionen durchzuskripten.
code: TEST
bezeichnung: Testflughafen (hindernisfrei)

# Kein echter Flughafen - taucht daher nicht in der Flughafen-Auswahl im
# Frontend auf (siehe backend/landung.py: flughafen_liste()). Über
# Spielplan("TEST") bzw. Landung("TEST") bleibt er trotzdem ganz normal
# nutzbar (siehe tests/test_engine.py).
sichtbar: false

module: null
faehigkeitskarten: 0

schwierigkeit: 0
laenge: 3

flugzeuge_wuerfel_kurven_min_max:
- 0 0 -2 2
- 0 0 -2 2
- 0 0 -2 2
